name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  # ── Repository structure check ────────────────────────────────────────────
  repo-check:
    name: Required files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify required files are present
        run: |
          required=(
            README.md
            ROADMAP.md
            CONTRIBUTING.md
            SECURITY.md
            .gitignore
            .env.example
            Makefile
            docs/dev-local.md
            docs/git-workflow.md
            .github/pull_request_template.md
          )
          missing=()
          for f in "${required[@]}"; do
            if [ ! -f "$f" ]; then
              missing+=("$f")
            fi
          done
          if [ ${#missing[@]} -gt 0 ]; then
            echo "Missing required files:"
            printf '  %s\n' "${missing[@]}"
            exit 1
          fi
          echo "All required files present."

  # ── Markdown lint ─────────────────────────────────────────────────────────
  markdown-lint:
    name: Markdown lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint markdown files
        uses: DavidAnson/markdownlint-cli2-action@v16
        with:
          globs: "**/*.md"
          config: ".markdownlint.json"

  # ── Shell script lint ─────────────────────────────────────────────────────
  shellcheck:
    name: Shell script lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run ShellCheck on scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: "./scripts"

  # ── YAML lint ─────────────────────────────────────────────────────────────
  yaml-lint:
    name: YAML lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install yamllint
        run: pip install yamllint

      - name: Lint YAML files
        run: yamllint -c .yamllint.yml blueprints/ .github/workflows/ infra/docker/

  # ── Go lint ───────────────────────────────────────────────────────────────
  go-lint:
    name: Go lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: apps/core/go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: apps/core

  # ── Go vulnerability scan ─────────────────────────────────────────────────
  go-vuln:
    name: Go vulnerability scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: golang/govulncheck-action@v1
        with:
          go-version-input: "1.22"
          go-package: "./..."
          work-dir: apps/core

  # ── Go Core tests ─────────────────────────────────────────────────────────
  go-test:
    name: Go Core tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: apps/core/go.sum

      - name: Run tests
        run: go test -v -race -count=1 ./...
        working-directory: apps/core

  # ── Go Core build ─────────────────────────────────────────────────────────
  go-build:
    name: Go Core build
    runs-on: ubuntu-latest
    needs: [go-lint, go-test]
    steps:
      - uses: actions/checkout@v4

      - name: Build Go Core Docker image
        run: docker build -t pcg-core:ci apps/core/

  # ── Node build (placeholder — enable when apps/web exists) ───────────────
  #
  # node-build:
  #   name: Node build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #     - uses: actions/cache@v4
  #       with:
  #         path: apps/web/node_modules
  #         key: node-${{ hashFiles('apps/web/package-lock.json') }}
  #     - run: npm ci
  #       working-directory: apps/web
  #     - run: npm run build
  #       working-directory: apps/web
