# Blueprints

Blueprints define installable apps.

A blueprint tells Cloud Core how to:

- create the app container
- route a subdomain to it
- health check it
- back it up
- start/stop it
- show it on the dashboard

See `docs/06-docker-and-app-blueprints.md`.
