# Oracle Cloud Infrastructure

Oracle Cloud is the primary production target.

Expected setup:

- Ubuntu Server VM
- public IPv4
- custom domain pointed to VM
- persistent storage mounted at `/var/lib/cloud-core`
- firewall allowing 80/443
- SSH restricted where possible
