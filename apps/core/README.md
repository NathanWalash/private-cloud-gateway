# Cloud Core Backend

The backend is the Go control plane.

Responsibilities:

- login/session auth
- `/api/auth/verify` for Caddy
- app registry
- Docker lifecycle management
- blueprint parsing
- health checks
- server metrics
- backup/restore
- audit log
- settings API
